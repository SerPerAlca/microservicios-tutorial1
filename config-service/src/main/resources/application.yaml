server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/SerPerAlca/microservicios-tutorial1.git
          search-paths: config-data
  application:
    name: config-service
